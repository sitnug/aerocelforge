# Services

The scientific orchestrator is the runnable service in this revision. The named
service directories reserve future process boundaries without duplicating the
working TypeScript kernels. Split a service only when isolation, language/runtime,
or deployment needs justify the operational cost.
