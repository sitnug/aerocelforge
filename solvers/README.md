# External solver integration

These directories are adapter boundaries, not bundled third-party source. Current
code lives in `packages/solver-adapters` and `services/scientific-orchestrator`.
Licenses and maturity are recorded in `THIRD_PARTY.md` and `ROADMAP.md`.
