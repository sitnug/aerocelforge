# Execution infrastructure

The templates here support a loopback local service, a Linux VM/container, or a
user-controlled remote Slurm host. They are starting points that require local
resource limits, pinned solver versions, authentication, TLS, observability, and
retention policies before shared deployment.
