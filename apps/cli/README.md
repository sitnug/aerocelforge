# CLI boundary

The project schema and engineering kernels are reusable from Node.js, but a stable
end-user CLI is not published in this revision. Add one only with machine-readable
result envelopes, deterministic exit codes, and the same solver safety boundary
as the desktop app.
