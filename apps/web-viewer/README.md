# Web viewer boundary

The Vite development build of `apps/desktop` already provides a browser preview.
A separate read-only deployment is reserved for a future artifact viewer with no
native filesystem or solver execution authority.
